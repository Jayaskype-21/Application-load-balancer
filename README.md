# Application-load-balancer
# It handle basic web traffic(HTTP,HTTPS)
# Distibuting incoming traffic across mutlipe instances(servers)based on specific rules,ensuring application fast,lowlatency
# It monitors server health
